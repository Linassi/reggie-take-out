server:
  port: 80

logging:
  level:
    com.cloudtech.dao: debug

spring:
  application:
    name:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://192.168.43.70:3306/reggie?serverTimezone=Asia/Shanghai&Unicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: manager
      password: Admin123.
  mybatis-plus:
    configuration:
      #在映射实体或者属性时，将数据库中表名和字段名中的下划线去掉，按照驼峰命名法映射
      map-underscore-to-camel-case: true
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    global-config:
      db-config:
        id-type: ASSIGN_ID
  mail:
    host: smtp.163.com
    protocol: smtps
    port: 465
    username: xx@163com
    password:
    from: xx@163.com
  redis:
    host: 192.168.43.70
    password: admin123.
    port: 6379
    database: 0
    jedis:
      pool:
        max-active: 8
        max-wait: 1ms
        max-idle: 8
        min-idle: 0
  cache:
    redis:
      time-to-live: 1800000

reggie:
  path: d://img/






